\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[english,russian]{babel}
\usepackage[margin=1in]{geometry}
\usepackage{noweb}
\noweboptions{}

\title{Пайплайн газовых тревог \\ \large грамотное описание (noweb / B3)}
\date{}

\begin{document}
\maketitle

\noindent
Это \emph{грамотная программа} в смысле Кнута. Первоисточник --- вот этот
документ: проза и код переплетены в порядке изложения мысли. Из него
\texttt{notangle} извлекает готовый к компиляции \texttt{.ml}, а
\texttt{noweave} --- этот самый PDF. Код, который вы видите ниже, и есть
тот, что запускается: между документом и шахтой нет расхождения по
построению.

\section{Задача}

Нам приходит поток показаний газа по зонам. Для каждой зоны действует
свой порог, и пороги меняются на ходу --- их присылает отдельный поток
уставок. Нужно поднимать тревогу, когда показание превышает
\emph{текущий} порог своей зоны, и снимать её, когда газ вернулся в
норму. Соберём это сверху вниз.

Скелет модуля --- доменные типы, затем три смысловые части и, наконец,
сборка всего пайплайна:

<<pipeline.ml>>=
open Miniflink

<<доменные типы>>
<<таблица порогов из потока уставок>>
<<условие и спецификация тревоги>>
<<сборка пайплайна>>
@ %def

\section{Доменные типы}

Три записи. [[reading]] --- сырое показание: зона и концентрация в ppm.
[[setpoint]] --- уставка: для какой зоны какой порог. [[checked]] ---
показание, уже дополненное действующим порогом; именно его увидит
правило тревоги.

<<доменные типы>>=
type reading  = { zone : string; ppm : float }
type setpoint = { sp_zone : string; max_ppm : float }
type checked  = { c_zone : string; c_ppm : float; c_max : float }
@ %def reading setpoint checked

\section{Таблица порогов}

Пороги живут не в коде, а в данных --- их присылает поток уставок.
Функция [[Table.build]] превращает этот поток в таблицу \guillemotleft{}зона $\to$
уставка\guillemotright{}, которая обновляется сама по мере прихода новых значений.
Ключ таблицы --- имя зоны.

<<таблица порогов из потока уставок>>=
let thresholds_of (setpoints : setpoint Mf_event.t Stream.t)
  : (string, setpoint) Table.t =
  Table.build ~key:(fun s -> s.sp_zone) setpoints
@ %def thresholds_of

\section{Условие и спецификация тревоги}

Поскольку порог теперь поле события, условие сравнивает два поля одного
[[checked]]: проблема, когда [[c_ppm]] выше [[c_max]]; снятие, когда
вернулось на уровень порога или ниже. [[Trigger.custom]] принимает ровно
такую пару предикатов.

<<условие и спецификация тревоги>>=
let gas_condition =
  Trigger.custom
    ~problem:(fun c -> c.c_ppm > c.c_max)
    ~recovery:(fun c -> c.c_ppm <= c.c_max)
@ %def gas_condition

Спецификация добавляет к условию тексты тревоги и снятия. Триггер сам
ведёт автомат на каждую зону: одна активная тревога на зону, снятие
парно тревоге.

<<условие и спецификация тревоги>>=
let gas_spec =
  Trigger.create ~name:"gas" ~condition:gas_condition
    ~produce_alert:(fun ~key ~value ~ts:_ ->
      Printf.sprintf "GAS %s: %.2f > %.2f" key value.c_ppm value.c_max)
    ~produce_recovery:(fun ~key ~ts:_ -> Printf.sprintf "CLEAR %s" key)
    ()
@ %def gas_spec

\section{Сборка пайплайна}

Теперь связываем всё в цепочку. Показания сперва оборачиваются в
[[checked]] с заведомо безопасным порогом [[infinity]] (пока настоящий
не подмешан --- тревоги не будет). Затем [[enrich]] подставляет
действующий порог из таблицы. Затем событие превращается в пару
[[(зона, checked)]], как ждёт триггер. И наконец [[Trigger.of_stream]]
выдаёт поток тревог.

<<сборка пайплайна>>=
let gas_alerts ~(setpoints : setpoint Mf_event.t Stream.t)
    (readings : reading Mf_event.t Stream.t) : string Mf_event.t Stream.t =
  let table = thresholds_of setpoints in
  readings
  |> Stream.map (Mf_event.map_value (fun r ->
       { c_zone = r.zone; c_ppm = r.ppm; c_max = infinity }))
  |> Pipe.enrich (module struct
       type t = checked
       let key c = c.c_zone
     end)
       ~from:table
       ~merge:(fun c sp -> match sp with
         | Some s -> { c with c_max = s.max_ppm }
         | None -> c)
  |> Stream.map (Mf_event.map_value (fun c -> (c.c_zone, c)))
  |> Trigger.of_stream gas_spec
@ %def gas_alerts

\end{document}
