# Генерируемые артефакты (исходники — .ml, .nw, weave.py; финальные PDF в репо)
*.aux
*.log
*.out
*.toc
*.tex
# .ml, извлечённый из .nw (B3): источник — .nw, не коммитим производное
gas_pipeline_b3.ml
