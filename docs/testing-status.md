# Отчёт о состоянии тестирования miniFlink

_Снимок на момент HEAD `9f0e25d`. Все цифры измерены на собранном
проекте (`dune build` + `dune test`, OCaml 4.14), а не оценены._

## Сводка

| Метрика | Значение |
|---|---|
| Строк библиотеки (`lib/*.ml`) | 5 579 |
| Модулей (`.ml` / `.mli`) | 42 / 36 |
| Тест-файлов | 120 |
| Property-файлов (QCheck) | 15 |
| Property-инвариантов | 63 |
| Покрытие строк | 86.03 % (2636/3064) |
| CI | активен, зелёный на OCaml 4.14 и 5.2 |

Состояние тестирования здоровое: покрытие выше типового порога 80 %,
ядро потоковой обработки покрыто property-инвариантами, exactly-once
проверяется property-тестом, конкуренция проверена на нескольких
уровнях (в т.ч. на OCaml 5.2 с настоящим параллелизмом), CI проверяет
обе версии OCaml на каждом пуше. Критических открытых рисков по
корректности нет.

## Покрытие по ключевым модулям

| Модуль | Покрытие | Заметка |
|---|---|---|
| `runtime_context.ml` | 100 % | domain-safe (Ctx_store v4/v5) |
| `stream.ml` | 98.8 % | ядро потоков |
| `trigger.ml` | 89.9 % | алерты/гистерезис |
| `merge.ml` | 88.9 % | слияние партиций, watermark |
| `temporal.ml` | 88.8 % | as-of join |
| `checkpoint_parallel.ml` | 85.9 % | exactly-once, барьеры |
| `window.ml` | 84.5 % | окна |
| `pipe.ml` | 83.6 % | операторы |
| `runtime.ml` | 77.4 % | DLQ/конфиг (остаток — сетевой код) |
| `metrics_log.ml` | 61.4 % | остаток — Prometheus HTTP + reporter-поток |

Просадки сосредоточены в инфраструктурных/observability-модулях
(Prometheus endpoint, reporter-поток — сетевой и потоковый код,
который намеренно не покрывается юнит-тестами, так как хрупок в CI), а
не в ядре потоковой обработки. Это разумный потолок без перехода к
интеграционному тестированию инфраструктуры.

## Property-инварианты (63)

Property-тесты проверяют свойство для тысяч случайных входов, а не для
подобранных примеров. Каждый инвариант сверяется с независимым oracle и
прошёл sanity-проверку (намеренная поломка инварианта → тест падает),
чтобы исключить «пустое» прохождение.

Покрытые классы:

- **Алгебра агрегатов** — count/sum/mean/median/count_if/to_list с
  retract равны пересчёту с нуля; `both` согласован покомпонентно.
- **Update ≡ Retract+Data** — для map/filter/flat_map и их safe-версий.
- **Триггеры** — ≤1 активный алерт на ключ; retract не превышает
  предыдущие данные; активные алерты = ключи выше порога.
- **Окна** — `window_agg` (sum/count/mean) и `window_fold`
  инкрементально равны пересчёту; `sliding`-назначение накрывает ts,
  выровнено по step, число окон = oracle.
- **Watermark-монотонность (P-02)** — watermark на выходе `merge`
  никогда не убывает при любых партициях, включая переход
  «молчала → ожила с отставанием». Роадмап называет это самым важным
  инвариантом streaming-системы.
- **Exactly-once при восстановлении** — для случайных потоков
  (Data+Watermark+Retract), произвольного числа воркеров и частоты
  чекпойнтов: recover+replay обрабатывает каждое Data ровно один раз
  (без потерь и дублей). Sanity-проверено — ловит исходный баг §4.1.
- **Temporal** — `as_of` = версия с максимальным `valid_from ≤ ts`;
  `prune` не меняет видимых ответов для `ts ≥ before`.
- **Прочие операторы** — `enrich` (left join), `session_window`
  (gap-разделение + сохранение событий), `update_table`
  (last-write-wins + passthrough), `dedup`, `keyed_join`,
  `process_keyed`, `silence_age`.

Операторы без property-инварианта — по сознательному решению: `stateful`
(нет универсального инварианта, зависит от пользовательской функции),
`keyed_join_map` (дублирует покрытый `keyed_join`), `count_window`
(близок к покрытому `tumbling`), `global_window` (сложный oracle,
средняя ценность).

## Конкуренция и параллельный путь

Параллельная обработка (`run_parallel_simple`, шардирование по ключу)
проверена на нескольких уровнях, причём CI прогоняет это на OCaml 5.2 с
настоящим параллелизмом доменов, где гонки реальны (а не маскируются
средой выполнения OCaml 4):

- **Инвариантность к числу воркеров** — один вход через 1/2/4/8
  воркеров даёт идентичный результат; гонка в диспетчере или
  шардировании проявилась бы как расхождение.
- **parallel = sequential** — параллельный прогон даёт то же
  мультимножество, что и последовательный (20 случайных потоков).
- **Контенция на общий sink** — 8 воркеров, малые каналы → форсированный
  backpressure; все события доходят без потерь/дублей.
- **Краш воркера** — не приводит к дедлоку диспетчера; выжившие воркеры
  доводят обработку; recovery после краша корректен.
- **Supervisor race** — параллельный запуск пайплайнов, все результаты
  на месте.
- **Retract под параллелизмом** — ретракты доходят до воркеров (не
  отбрасываются диспетчером).

## Границы памяти

- **Soak** — длительные прогоны `window_agg` и `dedup` с
  продвигающимися watermark: пиковая память выходит на плато, а не
  растёт с числом событий (окна закрываются, устаревшие ключи
  вычищаются). Ловит утечки состояния, невидимые быстрым unit-тестам —
  критично для minePASS, работающего сутками.
- **union / merge под перекосом** — доказано O(1) по памяти при
  экстремальной разнице скоростей входов (прирост 0 слов на 1M / 500k
  событий); pull-модель исключает неограниченный рост буфера.

## Что сделано в недавней работе

- **Exactly-once при восстановлении исправлен (§4.1)** — главный
  исторический долг. `cp_offset` теперь согласован со снапшотом
  (позиция в потоке по числу обработанных Data), что устраняет и промах
  при watermark-ах, и потерю in-flight хвоста на финальном барьере.
  Проверено на watermark/retract/crash/in-flight; CI зелёный на 4.14 и
  5.2.
- **Domain-safety для OCaml 5** — ambient-контекст вынесен в `Ctx_store`
  с вариантами v4 (`ref`) / v5 (`Domain.DLS`); CI подтверждает сборку и
  тесты на 5.2.
- **CI активирован** — матрица OCaml 4.14/5.2 + шаг покрытия;
  зелёный на каждом пуше.
- **Покрытие измерено и поднято** — впервые измерено (84.7 %), затем
  86.1 %; добавлен `scripts/coverage.sh` и `docs/coverage.md` (включая
  установку bisect_ppx из исходников без opam).
- **Реальный баг надёжности исправлен** — битый payload больше не
  обрывает поток: уходит в DLQ и пропускается (`make_stream_with_dlq`).
  Найден при работе над покрытием.
- **Микрооптимизации** — `count_window` (инкрементальная длина вместо
  `List.length`), `session_window` (без `Hashtbl.copy` на каждый
  watermark) — заметно при ~4096 ключах minePASS.
- **Ответы на ревью** — v2 и v3 разобраны по пунктам; реальные находки
  исправлены, ложные (несуществующие Kafka-модули, мнимая опечатка)
  опровергнуты по коду. Новые находки обновлённого v3 (N-1, N-5, N-7)
  закрыты: устаревшая doc `keyed_join`, рекурсия `union` (переписана в
  цикл против stack overflow + стресс-тест на 200k watermark),
  time-complexity `median`.
- **`?on_retract` для `process_keyed` (N-2)** — опциональный callback,
  симметричный `?on_update`: позволяет реагировать на Retract; по
  умолчанию поведение прежнее (обратная совместимость).
- **Property-инварианты** (49 → 63), закрыты структурные дыры:
  watermark-монотонность, temporal, session_window, window_fold,
  sliding, enrich, update_table, и **exactly-once при восстановлении**
  (разблокировано фиксом §4.1).
- **Конкуренция** — добавлены инвариантность к числу воркеров (1/2/4/8
  дают идентичный результат) и отсутствие потерь под контенцией на общий
  sink; вместе с существующими (parallel=sequential, краш воркера,
  supervisor race, retract под параллелизмом) проверяют параллельный
  путь, в т.ч. на OCaml 5.2.
- **Soak / границы памяти** — длительные прогоны на утечки (плато
  памяти) и доказательство O(1) для union/merge под перекосом.
- **Примеры** — ex07 переведён на атомарный `Update` для коррекций
  алертов (вместо пары Retract+Data, без None-flicker); добавлен ex11
  (учёт присутствия), где `Retract` нужен по сути — исчезновение без
  замены. Вместе показывают «Update для замены, Retract для удаления».

## Закрытые риски

1. **cp_offset / exactly-once (review v3 §4.1) — ИСПРАВЛЕН** (HEAD
   `47f8934`). Долгое время главный незакрытый долг. Оказалось два
   связанных бага:

   - *Исходный §4.1:* `cp_offset` брался из числа обработанных Data, а
     `source.seek` ждёт индекс в полном потоке
     (Data+Watermark+Retract+Update). При watermark-ах между Data offset
     промахивался — повтор или потеря событий.
   - *Глубже (вскрылся при починке первого):* финальный барьер
     фиксировал «сколько разослано», а снапшот содержит «сколько
     обработано»; при in-flight событиях offset обгонял снапшот. На
     потоках из одних Data старый код был согласован случайно, поэтому
     наивный фикс «offset = позиция источника» чинил случай с watermark,
     но ломал all-Data (терял хвост при replay).

   **Решение:** координатор ведёт `data_positions` (k-е Data → позиция в
   потоке); `cp_offset = data_positions[snap_processed]` — позиция,
   соответствующая числу Data в снапшоте. Offset согласован со снапшотом
   независимо от watermark-ов и in-flight; старые записи чистятся на
   каждом коммите (память ограничена на long-running прогоне — важно при
   ~4096 лампах).

   **Проверено:** all-Data (2000), Data+Watermark (400),
   Data+Watermark+Retract — recovery+replay ровно по счёту без потерь и
   дублей, по 3 прогона каждый; crash-тесты ×3; полный набор зелёный; CI
   зелёный на OCaml 4.14 и 5.2. `test_cp_offset` переписан из
   xfail-заглушки в строгую регрессию (согласованность offset/снапшот И
   полный recovery+replay = n).

## Что осталось вне текущего охвата (по приоритету)

- **Инфраструктурное покрытие** (Prometheus endpoint, reporter-поток) —
  честнее закрывать интеграционными прогонами, а не юнит-тестами; это
  основной остаток непокрытых строк.
- **`pipe.mli` (501 строка)** — разбиение на тематические части для
  читаемости. Чистая косметика, на корректность не влияет.

Ранее числившиеся здесь пункты закрыты: **exactly-once как property**
(добавлен property-тест recover+replay = бескрашевый прогон) и
**concurrency-тесты** (инвариантность к числу воркеров, контенция на
sink; прогоняются на OCaml 5.2 с настоящим параллелизмом).

## Вывод

Тестовая база в хорошем состоянии: покрытие ~86 %, 63 property-инварианта
(включая exactly-once при восстановлении), конкуренция проверена на
нескольких уровнях, CI зелёный на обеих версиях OCaml. Главный
исторический долг — корректность exactly-once (§4.1) — закрыт, проверен
на сценариях с watermark, retract, крашами и in-flight событиями, и
защищён property-тестом. Критических открытых рисков по корректности
нет; остаток (инфраструктурное покрытие, разбиение `pipe.mli`) — это
читаемость и observability, а не корректность.
